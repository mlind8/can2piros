#include "ros/ros.h"
#include "std_msgs/String.h"
#include "can_msgs/Frame.h"

#include <sstream>

void decode_cb(can::Frame canfr)
{
  ROS_INFO("look a message"); 

}

int main(int argc, char **argv)
{


  ros::init(argc,argv, "decan");
  ros::NodeHandle nh;

  ros::Subscriber sub = nh.subscribe("/received_messages",decode_cb);

  return 0;
}
